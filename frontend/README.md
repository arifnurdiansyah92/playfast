# Playfast Frontend

Next.js 16 + MUI 7 frontend for the Playfast game sharing platform.

## Setup

```bash
pnpm install
pnpm dev
```

Runs on http://localhost:3000. Requires the backend API at http://localhost:5000 (proxied via next.config.ts rewrites).
